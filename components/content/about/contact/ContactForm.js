import { useTranslation } from "next-i18next";

const ContactForm = () => {
  const { t } = useTranslation(["contact"]);
  return (
    <div>
      <form></form>
    </div>
  );
};

export default ContactForm;
